%h1 Search Page
%div
  Enter name to search for:
  %div
    = form_tag(input_search_path, method: "get") do
      = label_tag(:q, "Search for:")
      = text_field_tag(:q)
      = submit_tag("Search")
