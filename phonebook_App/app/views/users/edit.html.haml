%h1 Edit a user
%div
  = form_for(@user) do |f|
    = f.label :name
    = f.text_field :name
    = f.label :email
    = f.email_field :email
    = f.label :number
    = f.number_field :number
    = f.submit "Save changes", class: "btn btn-primary"
